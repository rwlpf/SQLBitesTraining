DECLARE @RowNo INT = 1;
DECLARE @MaxRowNo INT = 5;
WITH
ROWCTE
AS (
	SELECT @RowNo AS ROWNO
    UNION ALL
    SELECT ROWNO + 1
    FROM ROWCTE
    WHERE ROWNO < @MaxRowNo
	)
SELECT ROWNO
FROM ROWCTE;

